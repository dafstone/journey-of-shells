cat << EOF

Example 2: 

- Learn the difference between readline (.inputrc) and bash (.bashrc) and how they work together.
- See how the bash shell is configured!  (Try bind -v & bind -p)
- Good time to play a little with grep -v (unmatch) or grep -E (extended regexp)

EOF
